

# Lab 3. Cristian Eduardo Espinoza Silva


 * Para ejecutar debe ejecutar el makefile, colocando en la consola lo siguiente:
	make
 * En una segunda instancia, debe dirigirse a la carpeta Bin y ejecutar el ejecutable creado
	1. Dirigirse a la carpeta Bin : cd Bin
	3. ./ejecutable (Nombre de archivo de entrada) (Nombre de archivo de salida)
 * El archivo sera creado en la carpeta Out
	1. Para verificar la salida debe dirigirse a la carpeta Out : cd Out
	2. Luego debe abrir el archivo creado que contendra la solucion.

# Tener en consideración que las pruebas se hicieron en un Sistema operativo Linux.
